<div class="jumbotron jumbotron-fluid mb-0">
  <div class="text-center">
    <% if logged_in? %>
      <h2>ようこそ！<%= current_user.name %></h2>
    <% else %>
      <%= link_to "Signup", new_user_path, class: "btn btn-main btn-lg" %>
      <%= link_to "Login", login_path, class: "btn btn-main btn-lg" %>
    <% end %>
    <h4 class="mt-4">自分だけのオリジナル単語帳を作成しよう！</h4>
  </div>  
</div>
<nav class="navbar navbar-expand-sm navbar-dark text-center">
  <h3 class="text-center">How To</h3>
</nav>